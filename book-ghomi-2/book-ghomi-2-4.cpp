#include <stdio.h>

// Main function - the entry point of the program
int main() {
    printf("The answer to the question of Life \n") ;
    printf("the Universe, and Everything is 42") ;
    return 0;
}
