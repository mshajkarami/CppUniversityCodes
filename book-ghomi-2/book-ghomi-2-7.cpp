#include <iostream>
#include <cmath> // for pow(), though it's not used here

using namespace std;

int main() {
    int w;
    printf("Please enter a number of weight:\n");
    scanf("%d", &w);
    printf("Weight = %d\n", w * 10);
    return 0;
}
